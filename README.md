# mc-server-packs

Minecraft server resource-pack hosting.

Drop resource-pack `.zip` files into `mc-resource-packs/` and push to `main`. GitHub Actions merges them into one versioned release asset and writes the `server.properties` resource-pack lines in the release notes.
